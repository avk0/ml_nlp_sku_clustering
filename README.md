# ml_nlp_sku_clustering

Template Jupyter Notebook for text (sku) clustering problems. (Comments in russian)

* tokenization
* vectorization
* models: KMeans, dbscan, AgglomerativeClustering
* dendrogram visualization
